#!/usr/bin/env bash
set -euo pipefail

# Run the scanner from your repo

if ! scripts/scan_suspicious.sh overlays/ ; then
  echo
  echo "⚠ Suspicious content detected (non-ASCII). Proceeding anyway."
  # exit 0 to allow commit
fi


# Optional: you can add more checks here (shellcheck, shfmt, etc.)

echo "✓ Pre-commit checks passed."
